context("Tests for loading data from different formats")
library(SCORER)
library(readr)

test_that("Loading is done correctly from external data", {
  #expect_equal(loaddataset(system.file("extdata", "3Opt.csv", package = "SCORER")), opt_with_ranks)
  TRUE
})
